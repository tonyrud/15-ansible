# Module 15 - Ansible

## CMDs

ping

```bash
make ping
```

install and start nginx from hosts file

```bash
make nginx
```

Start node app on hosts

package node app:

```bash
make build_app
```

```bash
make deploy_app
```

See running app:

`<public-dns>:8009/api`
